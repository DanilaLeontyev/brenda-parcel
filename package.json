{
  "name": "brenda-parcel",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rm -rf .cache && rm -rf public && parcel src/markup/index.html --out-dir public -p 3000",
    "build": "rm -rf .cache && rm -rf dist && parcel build src/markup/index.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "normalize-scss": "^7.0.1"
  },
  "devDependencies": {
    "autoprefixer": "^8.6.5",
    "cssnano": "^4.0.1",
    "node-sass": "^4.9.2",
    "postcss-font-magician": "^2.2.1",
    "postcss-modules": "^1.1.0"
  }
}
